.topbar
	%ul
		%li
			%a{:href=>""} GAIA PDB
		%li 
			%a{:href=>""} Search
		%li 
			%a{:href=>""} Browse

		#flash.span5
			- flash.each do |key, msg|
				%p{:class => "#{key} flash"}
				= msg
			.clear

%div.top-space