.topbar
	%ul
		%li
			= image_tag('icrar_logo_black_small.jpg')
		%li
			%a{:href=>"#{root_path}"} GAIA PDB
		%li 
			%a{:href=>"#{search_parameters_path}"} Search
		%li 
			%a{:href=>"#{parameters_path}"} Browse

		#flash.span5
			- flash.each do |key, msg|
				%p{:class => "#{key} flash"}
				= msg
			.clear

%div.top-space